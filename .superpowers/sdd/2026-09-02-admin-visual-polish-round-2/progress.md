# Admin Visual Polish — Round 2 SDD ledger

## Task 1 — Font, admin badge treatment, Actions controls, specs widths, header controls

### Preflight
- User approved the proposed bounded implementation design in chat.
- Existing linked worktree: `feat/admin-redesign-shadcn-i18n`.
- `npm test` baseline launched as job `pwsh-95`.
- GitNexus impacts: `AdminProductsPage`, `ProductSpecificationsForm`, and `Badge` are LOW (0 callers/processes); `AdminLayout`, `LocaleToggle`, and `ThemeToggle` are absent from index.
- Badge audit: default `PublicBadge` is rendered by public product card/pages. The named `Badge` is currently imported only by Admin sources. Ruling: introduce an Admin-only semantic status Badge component and migrate only Admin uses, preserving the public default Badge and shared public type exports. This makes public visual isolation structural rather than dependent on import conventions. Cost if wrong: a missed Admin status use; targeted all-Admin import/source tests and review will detect it.
- Ruling: IBM Plex Sans is instantiated in the Admin layout and exposed only as `--font-admin-sans` on `#admin-theme-root`; CSS scopes the inherited body font and rescinds `.font-body` Inter declarations inside Admin, but leaves `.font-mono` unchanged. Cost if wrong: admin text could remain Inter; source-contract and runtime build tests guard it.
- Ruling: The plan says `existing icon + Dark/Light`, but current ThemeToggle has text only. Add existing project Lucide theme glyphs (not emoji) to meet the final visual spec while retaining the accessible label/text; cost if wrong is an import/render issue covered by typecheck and source test.

### RED evidence
- Added `tests/adminVisualPolishRound2.test.ts` before production edits.
- Command: `node --experimental-test-module-mocks --import tsx --test tests/adminVisualPolishRound2.test.ts`
- Result: 4 failures / 0 passes. The Admin layout lacked `IBM_Plex_Sans`; the Admin-only badge source was absent; Actions still used the previous wrapped layout; dark primary/header/grid controls still used prior values. The first preliminary run also confirmed the new Admin badge file was absent; the test was then made resilient so it asserted the absence as ordinary failures rather than a file-read error.

### GREEN implementation
- Added Admin-only `AdminStatusBadge` with a literal first-child 6px dot, low-opacity tint and border treatments in light/dark variants; migrated all named Admin lifecycle/access/index/derived/confidence uses only.
- Scoped IBM Plex Sans 500/600/700 to `#admin-theme-root` as `--font-admin-sans`; preserved root/public font setup and IBM Plex Mono labels.
- Rebuilt Products Actions into a no-wrap first row with separate sibling forms and 34px controls, then the specifications link alone on row two; field names, hidden values, actions, disabled and ARIA contracts are unchanged. The direct table `overflow-x-auto` and `min-w-[900px]` remain.
- Updated dark primary/ring to `#7C93AC`, dark primary foreground to `#101418`, widened Source Type/Confidence spans while keeping the 12-column total, and added exact segmented locale plus Lucide-icon theme control geometry.

### Verification
- Focused Admin command: `node --experimental-test-module-mocks --import tsx --test tests/adminProductPublishing.test.ts tests/adminI18n.test.ts tests/adminShadcnFoundation.test.ts tests/adminProductCreationPage.test.ts tests/adminVisualPolishRound2.test.ts tests/adminVisualDirectionWarmInkSlate.test.ts` — 35 pass, 0 fail.
- `npm test` — 333 pass, 0 fail, 8 skipped (341 total).
- `npm run typecheck` — pass. A first run exposed stale generated `.next` page types; clearing only generated `.next` and rerunning produced a clean pass.
- `npm run lint` — pass.
- `git diff --check` — pass.
- Impeccable detector command run over changed Admin targets. It returned one advisory only: pre-existing public `.bg-paper-grid` two-axis gradient in changed shared `src/app/globals.css` (line 79), which is out of scope and intentionally untouched; no Admin target finding.

### Review correction — brand semantic hue
- Review identified a semantic regression: `accessVariant.eligible` maps to `brand`, whose pre-change palette was green. The initial Admin-only implementation incorrectly assigned this status the slate primary hue.
- RED: updated `tests/adminVisualPolishRound2.test.ts` first to require `brand` light `rgba(34,197,94,0.08/.18)`, `#15803D`, `#22C55E`, and dark `rgba(74,222,128,0.10/.18)`, `#86EFAC`, `#4ADE80`. Command: `node --experimental-test-module-mocks --import tsx --test tests/adminVisualPolishRound2.test.ts`; result: 1 expected failure / 3 pass, showing the current slate `#445263/#7C93AC` implementation.
- GREEN: changed only `AdminStatusBadge` `brand` container and dot classes to that green semantic palette; retained geometry, mappings, all other variants, contracts, and public isolation. Updated the existing semantic palette assertions to match.
- Verification: `node --experimental-test-module-mocks --import tsx --test tests/adminVisualPolishRound2.test.ts tests/adminVisualDirectionWarmInkSlate.test.ts tests/adminProductPublishing.test.ts` — 21 pass, 0 fail. `npm run typecheck`, `npm run lint`, and `git diff --check` — pass.

### Whole-round review correction — Save emphasis and locale group semantics
- Binding review validation: the dark primary treatment requires a 700 Save weight only in dark mode, so `font-semibold dark:font-bold` preserves specified light 600 while applying dark 700. The locale wrapper already had an `aria-label`; adding `role="group"` exposes that label as the group name without changing the existing `aria-pressed` button selection behavior.
- RED: changed `tests/adminVisualPolishRound2.test.ts` first to demand both exact source contracts. Command: `node --experimental-test-module-mocks --import tsx --test tests/adminVisualPolishRound2.test.ts`; result: 2 expected failures / 2 pass, for missing `dark:font-bold` and missing `role="group"`.
- GREEN: added only `dark:font-bold` to the Products Save button and only `role="group"` to the LocaleToggle outer div; no actions, fields, i18n, status colors, table/scroller, or public sources changed.
- Verification: `node --experimental-test-module-mocks --import tsx --test tests/adminVisualPolishRound2.test.ts tests/adminVisualDirectionWarmInkSlate.test.ts tests/adminProductPublishing.test.ts tests/adminI18n.test.ts` — 29 pass, 0 fail. `npm run typecheck`, `npm run lint`, and `git diff --check` — pass. No dev server started (the prior stopped server remains stopped to release Prisma).

### Final review correction — confidence menu placement
- Review validation: Confidence renders its semantic badge directly below the trigger. The default Radix menu placement is below too, so the menu can paint over the badge despite wider grid columns. Setting only this `SelectContent` to `side="top"` moves the portal menu above while retaining Radix collision avoidance and the existing portal root.
- RED: added a focused source contract requiring `<SelectContent side="top">` in `tests/adminVisualPolishRound2.test.ts`, then ran `node --experimental-test-module-mocks --import tsx --test tests/adminVisualPolishRound2.test.ts`. Result: 1 expected failure / 3 pass because Confidence still used default placement.
- GREEN: changed only `SpecificationConfidenceSelect` to `<SelectContent side="top">`; source-type and all other selects retain their behavior. Names, default value, client state mapping, badge placement, and portal/root contracts are unchanged.
- Verification: `node --experimental-test-module-mocks --import tsx --test tests/adminVisualPolishRound2.test.ts tests/productSpecificationsForm.test.ts tests/productSpecificationsPage.test.ts tests/adminShadcnFoundation.test.ts` — 18 pass, 0 fail. `npm run typecheck`, `npm run lint`, and `git diff --check` — pass. No dev server started.

### Select consistency and badge-border specificity follow-up
- User provided live diagnosis and approved this bounded repair. Root cause is verified in current source: native lifecycle/source-type selects do not share the Radix UI used by Confidence, and `#admin-theme-root *` has ID specificity over badge tint utilities. Ruling: migrate only those two select instances to the existing shared Radix select and replace the wildcard selector with `:where(#admin-theme-root *)`; this fixes the cascade rather than adding `!important`, while preserving default token border behavior. Cost if wrong: a named Radix hidden native form input could differ from direct `<select>` submission; existing Radix form-contract tests plus focused server-action/spec tests guard it.

### Final automated verification and pending manual QA
- Fresh `npm run check` after the confidence correction passed: lint, typecheck, 333 pass / 0 fail / 8 skipped tests, Prisma generate, and production Next build all completed successfully.
- Final detector remains limited to the known pre-existing public `.bg-paper-grid` advisory in `src/app/globals.css`; no changed Admin target was flagged. `git diff --check 5ef5a92..87ca718` and current source diff are clean; only unrelated untracked `PRODUCT.md` remains.
- Pending required manual browser QA: both-theme font/badge/control rendering, 34px Actions measurement, non-clipped Source Type/Confidence text, confidence menu above badge, table scrolling/actions/hydration, and public visual isolation. The user approved stopping the worktree dev server at port 3200 to release the Prisma DLL and asked that it not be automatically restarted, so this session cannot collect those browser artifacts without a new user instruction.

### Select consistency and badge-border specificity implementation
- Impact basis: the user refreshed GitNexus impact results before this bounded change: `AdminProductsPage` LOW/0 and `SpecRowFields` LOW/0. No high/critical impact was reported.
- RED: extended the focused Admin publishing/specification/round-2 source contracts first, then ran `node --experimental-test-module-mocks --import tsx --test tests/adminProductPublishing.test.ts tests/productSpecificationsForm.test.ts tests/adminVisualPolishRound2.test.ts`. Result: 20 pass / 3 expected failures: missing Products Radix Select import/migration, missing `:where(#admin-theme-root *)`, and native Source Type markup.
- GREEN: migrated only lifecycle and Source Type to the existing shared Radix `Select`; lifecycle retains `status`, default status, label/trigger id, autofocus, status map, 34px `box-border`/7px/12px/13px-semibold geometry, action and sibling controls. Source Type retains its generated name/default, translated items and empty `SelectValue` placeholder (Radix reserves empty value for clearing, so no invalid empty `SelectItem` was introduced). Replaced the global selector exactly with `:where(#admin-theme-root *)`; no token was changed.
- GREEN focused command: `node --experimental-test-module-mocks --import tsx --test tests/adminProductPublishing.test.ts tests/productSpecificationsForm.test.ts tests/adminVisualPolishRound2.test.ts` — 23 pass / 0 fail.
- Relevant Admin command: `node --experimental-test-module-mocks --import tsx --test tests/adminProductPublishing.test.ts tests/adminI18n.test.ts tests/adminShadcnFoundation.test.ts tests/adminProductCreationPage.test.ts tests/adminVisualPolishRound2.test.ts tests/adminVisualDirectionWarmInkSlate.test.ts tests/productSpecificationsForm.test.ts tests/productSpecificationsPage.test.ts tests/productSpecificationsAction.test.ts` — 77 pass / 0 fail.
- `npm run lint` — pass. `npm run typecheck` was attempted but failed only on pre-existing stale generated `.next/dev/types/.../specifications/page.ts` (`createProductSpecificationsPage` export); cache clearing was deliberately not attempted because the user-owned HMR server on port 3200 is active. `npm run check`/build were not run per the Prisma DLL-lock constraint.
- Manual HMR QA (do not restart the existing port-3200 server): refresh the Admin products and specifications pages in both themes; confirm Lifecycle and Source Type open with shared Radix popup/icon behavior and preserve a selected/default value through Save. In DevTools on an Admin status badge, confirm `border-color` computes to its badge tint `rgba(...)` rather than `rgb(58, 50, 38)` / `#3A3226`; the matched default rule must be `:where(#admin-theme-root *)`, whose specificity is zero.
- GitNexus detector attempted with `npx gitnexus detect-changes --repo deskholt` immediately before commit; expected tool-version limitation: `error: unknown command 'detect-changes'`. No workaround was used.

### Source Type clear-selection regression repair
- Review found the prior Source Type migration incorrectly left no clearable Radix item: Radix prohibits an empty `SelectItem` value, while this form must submit `''` so the action persists `null`. `SpecificationSourceTypeSelect` is new and absent from the GitNexus index; the user directed this focused leaf implementation.
- RED: revised the incorrect source test to require a delegated client leaf, a non-empty private sentinel mapped to visual empty state, exactly one real hidden input, no Select `name`, and the unchanged generated name/default/labels pass-through. Command: `node --experimental-test-module-mocks --import tsx --test tests/productSpecificationsForm.test.ts tests/productSpecificationsAction.test.ts`; result: 35 pass / 1 expected failure because the client leaf import/file was absent.
- GREEN: added `SpecificationSourceTypeSelect`, a focused client leaf with private `__clear-source-type__` sentinel. It owns visual state from `defaultValue`, converts sentinel selection to `''`, leaves the shared Radix `Select` unnamed, and emits exactly one hidden named input containing only the real selected value. `SpecRowFields` now passes the existing generated source-type name/default/placeholder/translated labels into that leaf; Lifecycle, shared Select, CSS, Confidence, source units and actions were untouched.
- GREEN relevant command: `node --experimental-test-module-mocks --import tsx --test tests/productSpecificationsForm.test.ts tests/productSpecificationsAction.test.ts tests/productSpecificationsPage.test.ts tests/adminShadcnFoundation.test.ts tests/adminVisualPolishRound2.test.ts` — 47 pass / 0 fail.
- `npm run lint` — pass. `npm run typecheck` was attempted and remains blocked by the same user-HMR stale generated `.next/dev/types/.../specifications/page.ts` `createProductSpecificationsPage` export error; `.next` was not cleared and the port-3200 server was not stopped/restarted. `git diff --check` — pass.
- GitNexus detector attempted with `npx gitnexus detect-changes --repo deskholt` immediately before commit; expected unsupported command result: `error: unknown command 'detect-changes'`. No workaround was used.

### Index control no-wrap repair and historical audit
- User-confirmed minimal scope: only the index-toggle submit button in `AdminProductsPage` needed `inline-flex items-center justify-center whitespace-nowrap` to prevent full translated Enable/Disable labels from wrapping. Pre-edit GitNexus impact was LOW: 0 direct callers, 0 affected processes, 0 modules.
- RED: strengthened `tests/adminVisualPolishRound2.test.ts` before editing source to require the exact four classes and preserve the full `product.is_indexed ? translations.products.index.disable : translations.products.index.enable` mapping. `node --experimental-test-module-mocks --import tsx --test tests/adminVisualPolishRound2.test.ts` reported 3 pass / 1 expected failure because the source lacked those classes.
- GREEN: added only those four classes to the index button; retained its element, translated expression, `disabled`, ARIA description, forms, hidden values, command, geometry, colors, routes, and table structure. The focused test then reported 4 pass / 0 fail.
- Historical 8-point audit: completed in `index-nowrap-and-historical-audit-review-package.md`. Source and relevant contracts confirm Admin-only IBM Plex/public PublicBadge isolation; 6px dot+tint Admin badges and eligible green; two-row 34px Actions geometry and intact forms/i18n; dark Save tokens/700 weight; Source Type clear FormData and Confidence top portal; accessible header controls; zero-specificity `:where(#admin-theme-root *)`; and unmodified public/table/hydration/session/action contracts.
- Relevant suite: `node --experimental-test-module-mocks --import tsx --test tests/adminProductPublishing.test.ts tests/adminI18n.test.ts tests/adminShadcnFoundation.test.ts tests/adminProductCreationPage.test.ts tests/adminVisualPolishRound2.test.ts tests/adminVisualDirectionWarmInkSlate.test.ts tests/productSpecificationsForm.test.ts tests/productSpecificationsPage.test.ts tests/productSpecificationsAction.test.ts` — 77 pass / 0 fail. `npm run lint` and `git diff --check` passed. Impeccable detector on the changed Products source returned `[]`.
- `npm run typecheck` remains blocked solely by the user-HMR stale `.next/dev/types/.../specifications/page.ts` generated error for the pre-existing `createProductSpecificationsPage` factory export. `.next` was not cleared and port 3200 was not stopped/restarted. GitNexus `detect-changes` was attempted pre-commit and remains unsupported in this installed CLI (`unknown command`).
